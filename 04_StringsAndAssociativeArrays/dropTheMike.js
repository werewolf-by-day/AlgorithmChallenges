//create a standalone function that accepts an input string, removes leading and trailing white space, captializes the first letter of every word,
//and returns that string. if original string contains the word, "Mike" anywhere, immediately return, " a stunned silence" instead