//Print all values in a given array by iterating through it.

function printArrayValues(arr) {
	for(let i = 0; i < arr.length; i++) {
		console.log(arr[i]);
	}
}

printArrayValues([2, "lightsaber", 4, "mana", 5, "waffles"]);